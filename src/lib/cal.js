/**
 * Cal.com integration. Buttons carry data-cal-link + data-cal-config;
 * the embed (cal-embed.svelte) handles clicks on pages that include it.
 */
export const CAL_USERNAME = 'noelzappy';

export const CAL_EVENTS = {
	intro: '15min' // free 15-minute intro call
};
